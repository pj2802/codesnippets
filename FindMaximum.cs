using System;
using System.Linq;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = { 3, 9, 5, 2, 10, 9, 18 };
        int maxNumber = numbers.Max();
        Console.WriteLine(maxNumber);
        Console.ReadLine();
    }
}
